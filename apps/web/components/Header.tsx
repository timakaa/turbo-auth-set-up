"use client";

import { Button } from "@repo/ui";

const Header = () => {
  return (
    <header>
      <Button variant='outline'>
        <span className=''>Sign in</span>
      </Button>
    </header>
  );
};

export default Header;
