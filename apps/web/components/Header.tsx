"use client";

import { Button } from "@repo/ui";

const Header = () => {
  return (
    <header>
      <Button variant='default'>
        <span>Sign in</span>
      </Button>
    </header>
  );
};

export default Header;
